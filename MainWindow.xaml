<ui:UiWindow x:Class="Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Editor"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Window.CommandBindings>
        <CommandBinding Command="New" Executed="NewCommandBinding_Executed"/>
        <CommandBinding Command="Open" Executed="OpenCommandBinding_Executed" />
        <CommandBinding Command="Save" CanExecute="SaveCommandBinding_CanExecute" Executed="SaveCommandBinding_Executed" />
        <CommandBinding Command="SaveAs" Executed="SaveAsCommandBinding_Executed" />
        <CommandBinding Command="local:CustomCommands.Exit" Executed="ExitCommandBinding_Executed" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Ctrl+Shift" Command="SaveAs" />
    </Window.InputBindings>

    <DockPanel>
        <ToolBarPanel DockPanel.Dock="Top">
            <Menu DockPanel.Dock="Top">
                <ui:MenuItem Header="_Файл">
                    <ui:MenuItem Header="_Создать" Command="New" InputGestureText="Ctrl+N"/>
                    <ui:MenuItem Header="_Открыть" Command="Open" InputGestureText="Ctrl+O"/>
                    <Separator/>
                    <ui:MenuItem Header="_Сохранить" Command="Save" InputGestureText="Ctrl+S"/>
                    <ui:MenuItem Header="_Сохранить как..." Command="SaveAs" InputGestureText="Ctrl+Shift+S"/>
                    <Separator/>
                    <ui:MenuItem Header="_Выход" Command="local:CustomCommands.Exit" InputGestureText="Alt+F4"/>
                </ui:MenuItem>
            </Menu>
            <Separator />
        </ToolBarPanel>
        <avalonEdit:TextEditor
    xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
    Name="textEditor"
    FontFamily="Consolas"
    ShowLineNumbers="True"
    FontSize="10pt" Padding="10,10,10,10"/>
    </DockPanel>
</ui:UiWindow>
